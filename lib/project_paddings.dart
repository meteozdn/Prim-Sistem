import 'package:flutter/cupertino.dart';

class ProjectPaddings {
  static const mainHorizontalPadding = EdgeInsets.symmetric(horizontal: 25.0);
  static const midTopPadding = EdgeInsets.only(top: 25.0);
  static const smallTopPadding = EdgeInsets.only(top: 15.0);
  static const smallVerticalPadding = EdgeInsets.symmetric(vertical: 15.0);
}
